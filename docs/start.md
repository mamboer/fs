# Getting Started

## Install

```shell
npm install --save @aotu/fs
```

Or with `yarn`

```shell
yarn add @aotu/fs
```

## Access

```js
const { walk, flatten } = require('@aotu/fs');
// -OR-
const FS = require('@aotu/fs');

walk(...)
flatten(...)
// -OR-
FS.walk(...)
FS.flatten(...)
```
